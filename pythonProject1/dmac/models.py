from django.db import models

# Create your models here.
from django.db import models

# Create your models here.
class Lead(models.Model):
    table1=models.CharField(max_length=100)
    table2=models.CharField(max_length=100)
